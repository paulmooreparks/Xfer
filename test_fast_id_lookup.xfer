{
    <!id "root"!>
    name "Test Document"

    users [
        <!id "user1"!> { name "Alice" role "admin" }
        <!id "user2"!> { name "Bob" role "user" }
        <!id "user3"!> { name "Charlie" role "moderator" }
    ]

    config {
        <!id "app-config"!>
        debug ~true
        version "1.0.0"

        database {
            <!id "db-config"!>
            host "localhost"
            port *5432
        }
    }
}
